# Misc

This folder is for non-core project artifacts, such as:

- temporary exports
- scratch docs
- one-off assets
- migration notes

If something becomes important and reusable, promote it into `frontend/`, `backend/`, or `notebooks/`.
